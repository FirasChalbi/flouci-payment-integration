import TransformingNetworkClient from '../../../communication/TransformingNetworkClient';
import List from '../../../data/list/List';
import { PaymentData } from '../../../data/payments/data';
import Payment from '../../../data/payments/Payment';
import Callback from '../../../types/Callback';
import InnerBinder from '../../InnerBinder';
import { CreateParameters, IterateParameters, ListParameters } from './parameters';
export default class CustomerPaymentsBinder extends InnerBinder<PaymentData, Payment> {
    protected readonly networkClient: TransformingNetworkClient;
    constructor(networkClient: TransformingNetworkClient);
    /**
     * Retrieve all Payments linked to the Customer.
     *
     * @since 1.1.1
     * @deprecated Use `page` instead.
     * @see https://docs.mollie.com/reference/v2/customers-api/list-customer-payments
     */
    all: CustomerPaymentsBinder['page'];
    /**
     * Retrieve all Payments linked to the Customer.
     *
     * @since 3.0.0
     * @deprecated Use `page` instead.
     * @see https://docs.mollie.com/reference/v2/customers-api/list-customer-payments
     */
    list: CustomerPaymentsBinder['page'];
    /**
     * Creates a payment for the customer.
     *
     * Linking customers to payments enables a number of [Mollie Checkout](https://www.mollie.com/products/checkout) features, including:
     *
     * -   Keeping track of payment preferences for your customers.
     * -   Enabling your customers to charge a previously used credit card with a single click.
     * -   Improved payment insights in your dashboard.
     * -   Recurring payments.
     *
     * @since 1.1.1
     * @see https://docs.mollie.com/reference/v2/customers-api/create-customer-payment
     */
    create(parameters: CreateParameters): Promise<Payment>;
    create(parameters: CreateParameters, callback: Callback<Payment>): void;
    /**
     * Retrieve all Payments linked to the Customer.
     *
     * @since 3.0.0
     * @see https://docs.mollie.com/reference/v2/customers-api/list-customer-payments
     */
    page(parameters: ListParameters): Promise<List<Payment>>;
    page(parameters: ListParameters, callback: Callback<List<Payment>>): void;
    /**
     * Retrieve all Payments linked to the Customer.
     *
     * @since 3.6.0
     * @see https://docs.mollie.com/reference/v2/customers-api/list-customer-payments
     */
    iterate(parameters: IterateParameters): import("../../../plumbing/iteration/HelpfulIterator").default<Payment>;
}
