import TransformingNetworkClient from '../../../communication/TransformingNetworkClient';
import Chargeback, { ChargebackData } from '../../../data/chargebacks/Chargeback';
import List from '../../../data/list/List';
import Callback from '../../../types/Callback';
import Binder from '../../Binder';
import { IterateParameters, ListParameters } from './parameters';
export declare function getPathSegments(settlementId: string): string;
export default class SettlementChargebacksBinder extends Binder<ChargebackData, Chargeback> {
    protected readonly networkClient: TransformingNetworkClient;
    constructor(networkClient: TransformingNetworkClient);
    /**
     * Retrieve all chargebacks included in a settlement.
     *
     * @since 3.7.0
     * @see https://docs.mollie.com/reference/v2/settlements-api/list-settlement-chargebacks
     */
    page(parameters: ListParameters): Promise<List<Chargeback>>;
    page(parameters: ListParameters, callback: Callback<List<Chargeback>>): void;
    /**
     * Retrieve all chargebacks included in a settlement.
     *
     * @since 3.7.0
     * @see https://docs.mollie.com/reference/v2/settlements-api/list-settlement-chargebacks
     */
    iterate(parameters: IterateParameters): import("../../../plumbing/iteration/HelpfulIterator").default<Chargeback>;
}
